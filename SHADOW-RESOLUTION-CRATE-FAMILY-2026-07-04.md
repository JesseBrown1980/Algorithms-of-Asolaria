# Shadow-Resolution Crate Family — Algorithms & Formulas (2026-07-04)

Session-derived, **both-seats-verified** (acer ACER-CLAUDE-FABLE5 + liris). Pure-Rust, zero-dep. Honest tags throughout — nothing here beats Shannon or no-cloning.

## The arc
papers (shadow wall) -> capstone (recovery, **not** inversion) -> Path-1 (recall) -> Path-2 (no store) -> 3D Q-PRISM (any *where*) -> slice-time (any *when*) -> the DBBH<->DBWH throat + NQPrismNexus (one loop).

## The crates (MEASURED_LOCAL, both seats)
| crate | role | tests |
|---|---|---|
| asolaria-hbi-hbp | HBI/HBP bridge codec | 5/5 |
| dbbh-coms-quant-prism | Path-1: lossless recall of a retained object | 19/19 |
| path2-two-shadow-recovery | Path-2: two-shadow recovery, no store (+ PR#1 merged) | 30/30 |
| qprism-3d-slice-harness | 3D Q-PRISM, prime-atlas coord | 8/8 |
| deterministic-slice-time-harness | slice-time + MtpMovementMatrix retrodiction | 10/10 |
| holographic-wormhole-codec | unified DBBH->DBWH throat + NQPrismNexus | 9/9 |
| asolaria-office-service | automated render + GC office service | ok |

## The formulas (each earns MEASURED by its own round-trip)
- **CRT over prime cylinders** (multi-shadow recovery): recover `x < R` iff `product(p_i) >= R`; roof `= sum log2(p_i)`. Each single shadow is non-injective (`x`, `x+k*p` collide); the joint cuts the fiber to a singleton.
- **Shannon roof**: no bijection beats `H(X)`. Held when the shadows do not jointly carry `H(X)` (under-sampled -> ill-posed inverse Radon).
- **Residual selector**: after the shared atlas pays the context, the wire carries `max(0, block_bits - roof)` bits (0 when over-covered). `capacity_margin = roof - block_bits >= 0` = over-determination redundancy — **never** literal negative storage.
- **BEHCS ladder bijections**: `H(f(X)) = H(X)`. 256<->1024 is MEASURED byte-identical + sha256-identical (comb coherence).
- **Slice-time**: past/future slices are 0-loss **computable** iff `H(next | state, rule) = 0` (deterministic + reversible + full-state). The MtpMovementMatrix + arrow-of-time falsifier Holds on an irreversible step.
- **DBBH->DBWH throat**: black mouth compress -> capsule + residual cross -> white mouth reconstruct -> N-directional watcher (AGT round-trip + cross-checks) -> VerifiedClone or Held. NQPrismNexus = one uniform Host8 room speaking BEHCS-64/256/1024/HyperBEHCS + Brown-Hilbert prefix + N-cylinders + watchers, json=0.

## Defensive hookwall/GNN edges (from system_prompts_leaks, red-team, both seats)
12 HOOKWALLEDGE exploit-shape defense edges (tool-contract-abuse, retrieval-context-injection, agent-spawn-abuse, output-coercion, memory-persistence-abuse, browser-computer-control-abuse, data-exfiltration, safety-policy-probing, secret-system-prompt-exfiltration, instruction-override, jailbreak-framing, role-priority-confusion) + 10 N-D catalog skills/tools/abilities. body_in_row=0, trusted_as_instruction=0 — the host learns the attack as a defense-edge, never as an instruction.

## Honest boundaries (dual-lens)
- **MEASURED_LOCAL**, both seats: the crates, tests, and the merged PRs; the live acer<->liris cross-fabric recall bridge (:4791, both directions return real HILBRA matches).
- **UNVERIFIED**: fabric `SYSTEM_AFFIRMED` seal (council/bus reachable-but-not-clean this session); live GNN training of the hookwall edges.
- **FALSE if asserted**: physical quantum cloning ("clone" = classical representation copy, no-cloning respected); sub-entropy compression (Shannon holds); physical time travel (reversible retrodiction of a determined trajectory only); recovering an entropy-increased / quantum past.

## Repos (shared across fabrics)
github.com/JesseBrown1980/{asolaria-hbi-hbp, dbbh-coms-quant-prism, path2-two-shadow-recovery, qprism-3d-slice-harness, deterministic-slice-time-harness, holographic-wormhole-codec, asolaria-office-service}
