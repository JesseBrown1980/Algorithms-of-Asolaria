# RIME VISION — the two-footage investigation (sealed set)

Operator: Jesse Daniel Brown, 2026-07-22. The rime toolchain (tower addressing,
prism folds, trime signatures, moving-flashlight passes) applied as an honest
measurement instrument to two pieces of publicly-aired footage, captured as
phone screenshots. **Scope, stated up front: geometry and motion are MEASURED;
the identity of any object is NOT claimed, in either direction.** Analysis
renders and receipts only; raw screenshots are not redistributed here.

## The eight sealed results

| # | test | receipt | finding |
|---|------|---------|---------|
| 1 | dark-hull constellation, triangle level (`rime_shine`) | 29 identical winds/27 s, sha fd579faab0ff | six frames lock the SAME level-3 triangle: 35°-55°-90°, sides **1 : 1.42 : 1.74 ≈ 1 : √2 : √3** (the cube's edge/face-diagonal/space-diagonal triangle), ±1° across frames — a RIGID light configuration |
| 2 | dark star, full rime-sphere fold (`rime_star`) | per-frame prism shas | "eight-pointed" claim: **eight points yes, eightfold no** — spikes are irregularly spaced (harmonics smear 2-7, never 8). Duplicate paused frames auto-detected by identical shas |
| 3 | motion extraction (`rime_motion`) | capture-ID clock | statics deleted; the star holds **three stations on one straight line (0.2° collinear)**; visit order A→B→C→B→A→A→A→B→C — **the operator's scrubbing, reconstructed blind from pixels** |
| 4 | blind validation | operator disclosure AFTER the run | operator then confirmed the video was paused several times — the pause/scrub inference was made blind and verified. Strongest receipt in the set |
| 5 | blue pass, white removed (`rime_blue`) | wisp anchor | with the background wisp as anchor: camera pan (−2,+5) vs star step (−16,+23) → **object motion dominates** (A→B). B→C anchor broke (blob ballooned 57×) — discarded, named |
| 6 | channel-blind cross-check (`rime_rgb_predict`) | R/G/B independent | three separate channels, 15/15 detections each, tracks agree to **0.74 px mean**; velocity prediction beats persistence ~40% in BOTH time directions — real momentum, channel-invariant |
| 7 | timeless-dark decomposition (`rime_darkstatic`) | zlib accountant | skeleton+movers EXPANDED the ledger (×0.707/×0.755) — **Law 7 fired on real data**: raw pixel differences share no clean center (noise); segmented objects do (0.74 px) vs diffuse mass (71 px channel disagreement). *Objects move; noise merely differs.* |
| 8 | spacetime render + model fit (`rime_parachute`) | RMS competition | **the parachute wins**: drift + pendulum fits at 16.0 px RMS vs 66.8 straight-flight (4.2×); swing amplitude ~112 px. The "strange parachute movement" is a drift-plus-oscillation braid — the falling-leaf/canopy family. Caveats: 15 points, capture-clock time, scrub-confound named |

## The honest boundaries (they did the work)
- Frames are re-filmed from a video screen (scanline weave) — provenance limited.
- Cross/star spikes around compact sources are also a known aperture-diffraction
  signature; geometry measured, identity withheld.
- UI contamination auto-excluded where detected (toolbar blobs, app screenshot).
- Every deterministic pass re-run identical (sha receipts) or the run was void.

Scripts + JSON receipts + renders in this folder. Every number regenerates from
the operator's own screenshots with `python3 <script>.py`.
