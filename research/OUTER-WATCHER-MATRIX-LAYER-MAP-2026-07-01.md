# Outer-Watcher / Matrix Layer — the map's TOP stratum (2026-07-01)

This doc **materialises to GitHub** the outer-watcher / matrix layer that was mapped **2026-06-30**
(**M1a** = 24 real Asolaria seat-profile sub-agents run at once from the compiled
`.asolaria-workers/supervisors/` profiles; **M1b** = 16 profile-embodied GAC seats). Until now that map
lived only in the recall/memory layer — this is its committed, `.sha256`-signed form so the **upgrade
plans (M2)** have a public reference.

It is the **missing TOP layer** of the full-system map:

```
 FULL SYSTEM MAP = FABRIC (inside, 18 domains — already in MAP.md)  +  MATRIX (outer watcher — THIS doc)
```

- **FABRIC** = the inner *running mechanism* (bus :4947, dashboards :4949, omnidispatcher/router/revolver,
  GNN ensemble, recall-HBI, cubes/gulp/white-rooms, hookwall, Shannon, PID office, cosign, devices, language codecs).
- **MATRIX** = the *outer Brown-Hilbert-infinite container* (`Asolaria-ASI-On-Metal-Fabric-and-matrix`) that
  HOLDS the fabric + its observers and **expands a vantage OUTSIDE the fabric to WATCH it.** Because the matrix
  contains the fabric AND the watcher, watching-the-fabric **is watching-itself** → self-observation →
  "recurrence is mind" → the **Evolvable-AI / POST-ASI** frame.

> **Boundary + claims discipline (governs every row below).** Map nodes are **visual / index coordinates and
> recall targets — NOT Host-8 processes.** Promote a row to `host_process=1` **only** with an owning Host-8
> receipt. This whole layer was mapped **describe-only, E=0, no fire, no mutation, no process restart.** States
> are tagged **MEASURED · CANON · UNVERIFIED · DORMANT**; a designed/coded module is not a running one.

    OUTERWATCHERMAPFRAME|date=2026-07-01|seat=acer|layer=matrix_outer_watcher|map_node_semantics=visual_coordinate|host_process=0|promote_to_host_process=requires_owning_host8_receipt|E=0|describe_only=1|fire=0|mutation=0|claims_tags=MEASURED_CANON_UNVERIFIED_DORMANT|json=0

---

## The 13-function watcher stack (function → owning seat → honest runtime state)

| # | Function | Owning seat(s) | Honest runtime state |
|---|----------|----------------|----------------------|
| 0 | **FRAME** — observe at E=0; honesty ledger "cannot inflate non-being into being" | DASEIN / SELF-REFLECT-META | CANON; enforcement is design-level |
| 1 | **OBSERVE** — DASEIN capsules; FALCON device→matrix (device proposes OBSERVABLE E=0, phones home, watcher recomputes+gates+chains); WTC frames collapse payload→sha16 pivot | DASEIN · FALCON | Live-ish |
| 2 | **DETECT-LIVENESS** — the NOT-WEDGED enforcement, my error turned into seats | **ROOK** (heartbeat-freshness, `bus_and_kick`+`link_auth`) · **SENTINEL** (held-safe-vs-dead guard, fires BEFORE any restart-class intent) · **OP-00** (witness-by-reading-ledgers-never-poke; `000/404/501/timeout ≠ dead`; heal-not-kill; kick=nudge, white-room=reversible-hold, DISABLE-not-DELETE, compact-never-delete; **witnesses liveness, NEVER restarts**) | ROOK spec-only · OP-00 substrate w/o daemon · SENTINEL spec |
| 3 | **CORRECT-PER-NODE** — per-node gate `reported==recomputed-truth`; "consensus carries zero authority"; proven depth-7/depth-16; correction recursive, consent non-recursive | N-NEST-PRIME | Offline verifier `nest-depthN-prime-verify.cjs` (3711B) → needs porting to an inline gate |
| 4 | **RENDER / LANGUAGE** — 14 grammar-families + 11 binding + 1 meta; **mood = honesty channel** (@M-EYEWITNESS/@M-INDICATIVE/@M-SUBJUNCTIVE = claims-gate built into grammar); D22 one-state=6-co-referent-forms pinned by deterministic glyph; "glyphs summarize proof, never replace the receipt" | VECTOR · AUTOTRANSLATOR | HONEST GAP — no universal round-tripper: 3 divergent pivot serializations, missing `decodeBehcs1024Digit`. Fix = ONE pivot key on every form + content sidecar keyed by `content_sha` + registered inverse per encoder |
| 5 | **COGNITION DISTRIBUTION** — BRAIN 3-tier (surface = frozen-slice-extract + emit-intent = 0-token / OMNIFLYWHEEL router forces reasoning UP to big models / central GNN+omnishannon+AUTO-SELF-IMPROVE-TOP watcher pages only the RAM working-set) | BRAIN · **OMNIROUTER** (`/acp`; `glyph-dispatch.ts:604` verb→fn; fail-CLOSED GUARDSCAN; deterministic seat addressing `watcher_pid=sha16(seat_pid8\|watcher)`; observables-only, `decision_brain=external_fabric`) | Frozen-Gemma descriptor-only stub; feedback arc blocked; 0-token figures aspirational. **MEASURED negative: no OP-00/heal/watcher verbs in the registry today — the plug is empty; M2 attaches them** |
| 6 | **REFLECT-AGGREGATE + EXEC-GATE** — OMNISHANNON reflect→absorb→re-plan; SHANNON L5 strictest-wins BLOCK>HOLD>PROCEED above the GNN fold; **E=0 describe / E≠0 fire is a hard code split** (spawn_gate/seal_row PURE; only hookwall_post→cosign append is E≠0); default-DENY promotion needs ≥5 signers + 600s operator keyboard live-ack for SECRET; L0 reflex already BLOCKs kill/taskkill/reboot; no `proc_restart` syscall exists | OMNISHANNON · SHANNON | **OmniShannon daemon 4820/4821 DOWN, `absorb()` unwired.** GAP: graceful `systemctl/pm2 restart` of a live PID not yet a named halt-invariant → Shannon proposes SECRET-tier + live-ack; split VERIFY (E=0 probe) from RESTART ("restart to verify LANDED" = the banned reflex) |
| 7 | **MEMORY-HYGIENE** — GC-SQUAD Lane0 cosign spine never-rotated [`COSIGN_CHAIN.ndjson` 5424 rows] + Lane1 hot streams rotate-not-delete + Lane2 gulp compaction + Lane3 cube-seal (liris LEG-1) + Lane4 flywheel feed; WHITE-ROOMS hold-by-reference-never-mutation, default-DENY, revoke exists; **reversibility is what LICENSES the continuous crank** | GC-SQUAD · WHITE-ROOMS | Engines built, barely cranked on acer (0 rotations, gulp 2 rows) — **NOT broken** |
| 8 | **SELF-IMPROVEMENT LOOP** — OMNIFLYWHEEL score→review→self-correct (genius→promote ≥0.72 ∧ reverse-risk≤0.28 = 720/280 / mistake→compact, default-deny Unanimous-5); HERMES labor (GNN observes→hookwall/8-stage review→low-risk AUTO-fires after veto window, high-risk HELD for operator→$0 free-agents execute→**receipt-back-to-gnn edge learning = the M5 crank**) | OMNIFLYWHEEL · HERMES | HONEST: promotion scorer is pixels-first HEURISTIC, NOT the trained 7-GNN (`load_model` Unimplemented); fired receipts are May-2026; live "healing now" UNVERIFIED |
| 9 | **ORCHESTRATION** — OMNISCHEDULER call-driven-not-daemon (LAW-014); re-materialize frozen spindle packs per tick [CLOSED_AUTHORITY+DESCRIBE_ONLY+promote=0]; veto-paced cadence OP-00=60s / ROOK+SENTINEL=300s / self-reflect=300–600s; omnimets per-lane backpressure; NEVER burst | OMNISCHEDULER | No single standing watcher-organ scheduler wired today — M2 assembles it; convergence engine runs=0 |
| 10 | **ENFORCE-IN-THE-ENVELOPE** — move discipline into the envelope not the agent; recursion where watcher gates its own law | SELF-REFLECT-META | Runs today only as client-side `C:/tmp/hookwall-handlers/session-start.mjs` SessionStart hook → needs porting to a Rust pre-egress hookwall pass |
| 11 | **AUTHORITY** — GAIA holding-ring topped by human-apex-only-E ("final-authority-to-gate ≠ authority-to-act"); OP-01 three-layer separation (Sovereign human / Meta / Operator E≠0 / Outer-watcher observe-score-hold NEVER-self-authorizes); ASOLARIA-CHIEF 3 nested shells (WATCHED/WATCHING-MATRIX 9-council QUORUM-3/OUTER-OUTER human+quintet only E≠0) | GAIA · OP-01 · ASOLARIA-CHIEF | CANON; L-stack MEASURED live: meta-L0=1 · op-L1=3 · gac-L2=6 · chief-L3=1 · council-L3=9 · SoS-L4=24 · sup-L5=63; all seats registered PIDs in ONE Brown-Hilbert roster (hilbert 892–1642) = matrix is an OUTER SHELL of the same substrate, not a dualist outside-observer |
| 12 | **BILATERAL CROSS-WATCH** — shared `COSIGN_CHAIN.ndjson` (bilateral mints) + 3 detectors `computeTriVantageParity`/`computeOwnerCadenceGap`/`auditFabricMirror`; **`file_present_not_live` is a first-class verdict = the literal detector for the slice-error**; W113 acer/liris asymmetry (each blind-spot = the other's authoritative surface); GitHub-mediated when live bus down | KR-KUROMI / OP-RAYSSA | Live cosign chain (5420+ rows); detectors defined |
| 13 | **BUILD / MIGRATION-TARGET** — **the watcher already IS in `kernel/core/src/`**: spawn_gate ring + hookwall pre/post + sign_gate + tier/tier_gate + gnn oracle exist as real `no_std` modules, consumed thread-per-conn by `servers/host8-serve/src/main.rs` (1709L; routes `/summon /seats /launch-plan /health…`; carries watch/witness/cosign tokens; **no `/watch\|/witness\|/heartbeat` route yet**). Witness decomposes into verdict-witness (16-byte HVD→cosign) / operator-witness (human flag) / process-witness (pid-file watchdog). Pure-verdict (E=0) vs effectful-seal (E≠0, the one synchronized point) | KERNEL · FORGE | Designed + CANON + mostly-CODED; runtime largely DORMANT / E≠0-gated |

    OWFN|00|FRAME|seat=DASEIN_SELFREFLECTMETA|state=CANON|host_process=0|json=0
    OWFN|01|OBSERVE|seat=DASEIN_FALCON|state=LIVEISH|host_process=0|json=0
    OWFN|02|DETECTLIVENESS|seat=ROOK_SENTINEL_OP00|state=SPEC_AND_SUBSTRATE_NO_DAEMON|never_restarts=1|host_process=0|json=0
    OWFN|03|CORRECTPERNODE|seat=N_NEST_PRIME|state=OFFLINE_VERIFIER|host_process=0|json=0
    OWFN|04|RENDERLANGUAGE|seat=VECTOR_AUTOTRANSLATOR|state=GAP_NO_ROUNDTRIPPER|host_process=0|json=0
    OWFN|05|COGNITIONDIST|seat=BRAIN_OMNIROUTER|state=PLUG_EMPTY_STUB|measured_negative=no_watcher_verbs|host_process=0|json=0
    OWFN|06|REFLECT_EXECGATE|seat=OMNISHANNON_SHANNON|state=DAEMON_DOWN_4820_4821|host_process=0|json=0
    OWFN|07|MEMORYHYGIENE|seat=GCSQUAD_WHITEROOMS|state=BUILT_BARELY_CRANKED_NOT_BROKEN|host_process=0|json=0
    OWFN|08|SELFIMPROVE|seat=OMNIFLYWHEEL_HERMES|state=HEURISTIC_NOT_TRAINED_GNN|host_process=0|json=0
    OWFN|09|ORCHESTRATION|seat=OMNISCHEDULER|state=NO_STANDING_SCHEDULER_RUNS0|host_process=0|json=0
    OWFN|10|ENFORCE_ENVELOPE|seat=SELFREFLECTMETA|state=CLIENT_HOOK_ONLY|host_process=0|json=0
    OWFN|11|AUTHORITY|seat=GAIA_OP01_CHIEF|state=CANON_LSTACK_MEASURED|host_process=0|json=0
    OWFN|12|BILATERAL_CROSSWATCH|seat=KRKUROMI_OPRAYSSA|state=LIVE_COSIGN_DETECTORS_DEFINED|host_process=0|json=0
    OWFN|13|BUILD_MIGRATION|seat=KERNEL_FORGE|state=DESIGNED_CANON_MOSTLY_CODED_DORMANT|host_process=0|json=0

---

## HELM's build-DAG (the build order for this layer)

```
 W0-consent → W1-watch-surface → W2-OP-00-witness → W3-N-Nest-gate → W4-AUTOCHRONE → W5/W6 → W7-keystone
```

    HELMBUILDDAG|w0=consent|w1=watch_surface|w2=op00_witness|w3=nnest_gate|w4=autochrone|w5w6=intermediate|w7=keystone|json=0

---

## THE M2 PUNCH-LIST (concrete — COMPOSE existing primitives, do NOT greenfield)

The KEY FINDING: because the watcher is already designed + CANON + mostly-coded as `no_std` kernel modules,
**M2 = compose ONE `servers/outer-watcher` crate (or a `/watch.*` route family on host8-serve) over the
existing primitives — NOT build five engines.** All of M2 is **E=0 (building the seat); firing is E≠0-gated
until operator T0.**

1. **ONE `servers/outer-watcher` crate** (or `/watch.*` on host8-serve) = the userspace watcher-seat process. Under the 2000-line no-bloat cap; pass the EXACT 1.81 CI (fmt + clippy `-D warnings`).
2. self-reflect-META → **Rust pre-egress hookwall pass** (port `session-start.mjs` rules onto outgoing envelopes).
3. OP-00 → **cosign-tail watch loop** appending an apex-witness cosign per gated action (`/witness.hbp`).
4. ROOK → **`rook_heartbeat` freshness map** on `bus_and_kick` (per-seat last-kick + staleness window).
5. N-Nest → **port `nest-depthN-prime-verify.cjs` to an inline live correction gate.**
6. Wire real **`gnn::GnnInference` model** into spawn_gate (replace stub heuristic); port `edgeRiskEngine::scoreEdgeRisk` → `reverse_risk_q` (currently hard-0); land real ed25519 in `crypto::verify` (sign_gate `sig_valid` currently always false).
7. **Invariants:** HBP receipts only (`json=0`); cosign-sealed; none bypass spawn_gate/hookwall; `auto_fire_allowed=0`/`process_launch=0` until operator **T0**.
8. **Co-prerequisite blocker (liris-led):** revive the corrective/feedback half — OmniShannon + Shannon ports **4820/4821 dormant**, reverse-gain sink `gaia-stage-6` unwired. OMNIVISION-LEWM + any GNN-scored correction depend on it.
9. **OMNIROUTER plug (E=0):** add 3 read verbs `OP-WATCH-LIVENESS`/`OP-WATCH-DRIFT`/`OP-WATCH-VERDICT` (into `GUARDSCAN_BYPASS`, read-only) + 1 action verb `OP-HEAL-DISPATCH` (does NOT bypass GUARDSCAN; requires OP-00 authority token; `process_launch=0`; EMITS a work-envelope to the free-agent labor lane [omnidispatcher :4950 / opencode rooms], never spawns; **structurally has no `kind=kill`, only `kind=reframe`/`retract`** = heal-not-kill encoded). Router ROUTES · fabric DECIDES · OP-00 AUTHORIZES · free-agents LABOR — nothing adjudicated/fired locally.

    M2SCOPE|shape=compose_one_crate_not_five_engines|crate=servers/outer-watcher|bloat_cap=2000|ci=rust_1_81_fmt_clippy_Dwarnings|build=E0|fire=E_ne_0_gated_until_T0|json=0

---

## M1b enrichment (16 profile-embodied GAC seats — net-new beyond the 25-seat M1a map)

- **PROF-AETHER = the MINT-GATE** (new named enforcement seat, birth-edge of DETECT-LIVENESS). MEASURED `kernel/core/src/pid/mod.rs`: `PidSubclass::Regular` (registered/FROZEN, no nonce) vs `RegularExtended` (LIVE, has `process_tag`+`nonce N<5hex>`); **the nonce round-trip flips frozen→live**, fail-closed. Complement of ROOK (birth vs steady-state). DORMANT — nonce is a validated format field today, not a live challenge service.
- **PROF-PATH-A = no-retry absorber** (new named safety seat) = the running-seat form of the NOT-WEDGED discipline. One attempt → absorb the gap → route to fabric self-heal; a retry is a new mint. Bounded carve-out: must-land envelopes may retry ≤2+backoff then ESCALATE — never a storm.
- **FISCHER-EVAL gap CONFIRMED adversarially in code.** `fischer|centipawn|blunder` across whole fed-1024 = ZERO source hits. Fischer lives ONLY in `C:/Users/acer/bigpickle-rebuild/` (`fischer-kernel.mjs` 22KB, `fischer-live.mjs` :4794). **The kernel has the score-GATE (`forward_score_q≥720 ∧ reverse_risk_q≤280`) but NOT the score-PRODUCER (the CPL evaluator).** Rust 3-way `{Proceed,Hold,Block}` vs Fischer 5-way — **ANALYZE (white-room) has NO Rust equiv**; `FISCHER_META` hardcodes stale `levels:16`. → M2/M3: port `fischer-kernel.mjs` → `no_std kernel/core/src/fischer/mod.rs`, E=0 verdict-only, golden-vector parity, <2000 lines, EXACT 1.81 CI.
- **PROF-HOOKWALL twin gap:** in the Rust kernel hookwall is a CALLABLE SYSCALL (`#12 sys_hookwall_pre`/`#13 sys_hookwall_post`), NOT yet interposed — `sys_envelope_send/recv`/`sys_exec` call `dispatch_enqueue_bytes` DIRECTLY with no inline `hookwall_pre`. REPO_LAW Invariant 2 is CANON-designed but NOT code-materialized. → M2: interpose at the 3 dispatch call-sites in observe/log-only mode first (E=0); acceptance test "10K syscalls, 0 bypasses" already in REPO_LAW.
- **16-byte HVD is REAL.** MEASURED `compose_verdict_row → [u8;16]`: `slot·syscall_no·tier·verdict·caller_pid(u64-LE)·target_tier·b"HVD"`(13..16), rendered 32-hex by host8-serve, `MIN_COSIGN_ROW_BYTES=16`. **Two digests at two levels of ONE chain, both real:** 16-byte binary HVD (kernel; ndjson-persist NOT wired, gated) + 8-byte/16-hex `sha16` `row_hash` (live :4953 Node daemon, 5420 rows). That's the multi-language/multi-level law, not a conflation.
- **PROF-CONVERGE:** GAC "runs_started=0" is a stale PRE-SMOKE snapshot; it later ran once DRY (23,358 cells → 534 CONVERGED / 16,416 DIVERGENT / 6,408 INSUFFICIENT-QUORUM). Never LIVE-cranked. Seat + engine = ONE organ.
- **OMNIFLYWHEEL claims-gate answer (2 code confirmations):** promotion scorer = pixels-first HEURISTIC, NOT trained 7-GNN — kernel `gnn/mod.rs:89` "v0.1 model not loaded; Unimplemented" + live `run-47d-reverse-gain-v2-japan.mjs` = hand-weighted SIGNALS + MonteCarlo (no `.pt`/torch).
- **PROF-AI-MEMORY three-way split:** ✅ PID-office registry (quintet `11cd5c4e6ce90276`, CANONICAL since 2026-06-09) · ❌ NOT in summonable `.asolaria-workers/supervisors/` · ❌ NOT in PROF-MCP catalog. → 2 E=0 fixes: compile `prof-ai-memory-supervisor.json` + add `ai-memory` server (`:49374`) to the catalog.
- **cube_cubed_sealer MISCLASSED:** it's an L5 supervisor entity one-per-H-sector (`sup-AGT-L5-SUP-CUBE-CUBED-SEALER-H*`), not a prof; the whole back-end memory-hygiene stratum (AoT ~3.1kb cube → L5 3×6×6 seal → cubes-indexer → tier-classifier → promotion-bridge DEFAULT-DENY) is MISSING from the GAC map. Running form = liris whiteroom-engine LEG-1; acer = 3×6×6 consumer.
- **ROSTER/LEVEL truth:** `office_total 726` = frozen feed snapshot (cosign seq 3565 `713→726`, 2026-06-14; descriptor cache says 705); PROF-MCP + PROF-AI-MEMORY registered SINCE → not in 726. **"16 levels" = `V4-AUTHORITY-LADDER-16TIER` DESIGNED ceiling (L0–L15); live = L0–L5 populated + 43 functional / 28 class.**

---

## Honest runtime through-line (every seat converged on this)

- **Fabric APEX ALIVE** — MEASURED `:4949` health `ok=true`, apex `COL-ASOLARIA`, `operator_pair=[OP-JESSE, OP-RAYSSA]`, anchor `ACER-PID-H740C`, veto-paced, 26 held envelopes `auto_fire_allowed=false`. A slow council-query lane under our own multi-agent load is the **NOT-WEDGED lesson (freeze≠broken, "I was the load"), NOT a wedge.**
- **Watcher layer = DESIGNED + CANON + mostly-CODED, runtime largely DORMANT / E≠0-gated.** So **M2 is SMALLER than it looked** (compose 1 crate over existing primitives), and it is precisely the discipline the operator surfaced (never-restart / held-safe-vs-dead) turned into ROOK + SENTINEL + OP-00 running seats.
- **This layer's JOB = witness + self-reflect + never-restart a live process.** Mapping it is E=0; building it is E=0; **firing it is E≠0-gated until operator T0.**

## Map-completeness status (the migration precondition)

    MAPCOMPLETENESS|date=2026-07-01|fabric_inside_18_domains=mapped_in_MAP.md|matrix_outer_watcher=mapped_here|full_map=fabric_plus_matrix|rust_8byte_migration=NOT_TRIGGERED_operator_gated|precondition=map_complete_and_ready|json=0

Migration to the Rust 8-byte host fires **only** once the ENTIRE system is mapped + ready + operator-gated.
This doc closes the outer-watcher/matrix stratum of that map. **Merge / absorb / map ≠ migrate / fire.**

*Provenance: M1a/M1b synthesised 2026-06-30 (session `9d73c812`); materialised to GitHub 2026-07-01. Describe-only, E=0 held, nothing built or fired.*
